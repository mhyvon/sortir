{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block main %}
    <h1>Site</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ site.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ site.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('site_index') }}" class="btn btn-primary">Revenir à la liste</a>

    <a href="{{ path('site_edit', {'id': site.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('site/_delete_form.html.twig') }}
{% endblock %}
